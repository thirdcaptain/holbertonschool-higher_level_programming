#!/usr/bin/python3
"""
module defines is_same_class function that returns True if object is a class
"""


def is_same_class(obj, a_class):
    """
    returns True returns True if object is an instance of a class
    False if otherwise
    """
    if isinstance(object, a_class):
        return True
    else:
        return False
